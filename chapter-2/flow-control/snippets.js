// Bindings
let name = "Chris"
const greeting = "Hello " ;
console.log(greeting + name);

//Return values
let x = 5, y = 10;
console.log(Math.max(x, y));
console.log(Math.min(x, y));
console.log(Math.min(x, y) * 100)

if (false != true) {
    console.log("That makes sense.");
    if (1 < 2) {
        console.log("No surprise here.");
    }
}